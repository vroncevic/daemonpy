name: daemonpy_toc
on:
  push:
    branches: [ main ]
    paths:
      - '**.md'
jobs:
  generateTOC:
    name: daemonpy_toc
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4